package class2;

public class Class2review {
	
	/* to organize code press ctrl+shift+f
	 * 
	 * // Java class 2 review
	 * 
	 * Declare a variable num1
	 * 
	 * Then we assign/initialize value to the variable
	 * num1=6;
	 * 
	 * declare multiple variable all together or in same time
	 * int num, num2, num4
	 * String str1, str2, str3;
	 * 
	 * We only can declare the variable once but we can reassign the value.
	 * num1 = 10;
	 * 
	 * Also we can use an other variable value to initialize other variable.
	 * num = num1;
	 * 
	 * We also Learned Concatenation: when we want to add an other variable to string we use
	 * (+) sign
	 * Exp: str = "goodevening";
	 *      System.out.println(str +num)
	 *      
	 *  We learned Operators in Java:
	 *  Assignment operator =
	 *  Arithmetic Operators +, -, *, % 
	 * 
	 */
}
